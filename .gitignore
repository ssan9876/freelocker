/bin/
*.exe
/web/node_modules/
/web/dist/
/web/test-results/
/web/playwright-report/
sim-state.json
# Built console bundle is generated by `npm run build` and by the Docker
# web stage; only the embed placeholder is tracked.
/internal/server/webui/dist/*
!/internal/server/webui/dist/.gitkeep
/server.yaml
# Server master key, written on first run; never commit a key.
master.key
